import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    paddingBottom: 20,
  },
  title: {
    paddingTop: 25,
  },
  button: {
    paddingRight: 10,
    paddingLeft: 10,
  },
}));
